name: Validate Repo

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate-repo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - name: Run repository guardrails
        run: node scripts/validate_repo.js
      - name: Run repository guardrail mutation suite
        run: node scripts/test_validate_repo.js
      - name: Run workspace instructions behavior suite
        run: node skills/workspace-instructions/test/test_workspace_instructions.js
      - name: Run validator test suite
        run: node skills/validator/test/test_recursive_scopes.js
      - name: Run vault registration test suite
        run: node skills/vault/test/test_register_vault.js
      - name: Run scope entry route contract
        run: node skills/vault/test/test_scope_entry_route.js
      - name: Run scope topology template contract
        run: node skills/vault/test/test_scope_topology_templates.js
      - name: Run research template contract
        run: node skills/vault/test/test_research_templates.js
      - name: Run scope topology suite
        run: node skills/validator/test/test_scope_topology.js
      - name: Run scope topology failure suite
        run: node skills/validator/test/test_scope_topology_failures.js
